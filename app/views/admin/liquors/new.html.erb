<%= content_for :title, "Add liquor — Admin" %>
<div class="animate-rise pb-10">
  <h1 class="font-display text-2xl font-semibold tracking-tight text-brand-primary sm:text-3xl">Add liquor</h1>
  <p class="mt-2 text-sm leading-relaxed text-brand-muted">
    Use a consistent name customers will recognise; quantity is whole bottles (or equivalent units).
  </p>
</div>

<%= render "form", liquor: @liquor %>
